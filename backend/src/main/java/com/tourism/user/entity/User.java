package com.tourism.user.entity;

/**
 * Placeholder – the canonical User entity lives in com.tourism.auth.entity.User
 * which is mapped to the `users` table.  This class is intentionally NOT an
 * @Entity to avoid a duplicate-table-mapping conflict.
 *
 * If user-package features are added in future, reference com.tourism.auth.entity.User
 * directly or create a distinct table.
 */
public class User {
    // intentionally empty – see com.tourism.auth.entity.User
}

